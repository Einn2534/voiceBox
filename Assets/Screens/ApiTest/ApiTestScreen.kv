# 
# main
#   generated by nullptr on 2025/10/10
# 

<ApiTestScreen>:
    BoxLayout:
        orientation: 'vertical'
        ScrollView:
            Label:
                id: apiResponse
                text: "ApiTestScreen"
                size_hint_y: None                     # 高さを自動で設定するために固定
                height: self.texture_size[1]          # テキストの高さに合わせる
                text_size: self.width, None           # 折り返しを有効にする（横幅に合わせて改行）
                halign: "left"
                valign: "top"

        BoxLayout:
            TextInput:
                id: apiRequest
                hint_text: 'apiRequest'
            Button:
                text: "Enter"
                on_press: root.send_text()
        Button:
            text: "Top"
            on_press: app.root.current = 'home'